# Copy to .env.local for local development
# In production, set this in Vercel / Netlify environment variables

# Point to your deployed Spring Boot backend
VITE_API_URL=https://your-backend.railway.app

# For local dev — leave blank (Vite proxy handles /api → localhost:8080)
# VITE_API_URL=
